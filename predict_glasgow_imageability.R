#!/usr/bin/env Rscript

message(
  "predict_glasgow_imageability.R is now a compatibility wrapper. ",
  "Use analysis/train_imageability_model_bundle.R as the canonical training entry point."
)

source("analysis/train_imageability_model_bundle.R")
